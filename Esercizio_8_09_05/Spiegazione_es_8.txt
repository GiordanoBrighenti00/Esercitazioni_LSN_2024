Funzione al variare dei parmateri: problema 1D: particella qtistica in potenziale con doppia buca: V(x).
I parametri sono contenuti nella funzione d'onda PSI: essi sono sigma e mu. 
8.1.	E' richiesto di campionare |PSI|^2
con Metropolis (ricorda che la normalizzazione è già 1) per poter calcolare la funzione <H>, che è quella da minimizzare
al variare dei parametri sigma e mu. E' opportuno effettuare il data-blocking per stimare come si deve l'incertezza:
essa ci servirà per capire quando convergerà l'ottimizzazione!!! 
Osserva inoltre che: H*(psi(x))/(psi(x)) = V(x) - 0.5*(psi''(x))/(psi(x))
8.2.	Implementare il simulation Annealing per ottimizzare mu, sigma. Devo cioè creare un altro algoritmo Metropolis.
Il campionamento dei punti che cercano il minimo globale devono disporsi su un istogramma che segue la wavefunction analitica
Nota: Mi aspetto che il minimo locale arrivi a -0.44